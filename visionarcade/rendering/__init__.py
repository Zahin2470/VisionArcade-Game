"""Rendering layer: window/surface management, HUD, themes, effects.

Only `renderer.py` (the base drawing surface + window lifecycle) is
implemented in Phase 1. HUD, particles, transitions, themes, and
typography are added in later phases as each game/UI needs them.
"""
